import { BrowserRouter as Router } from 'react-router-dom';
import Layout from './componets/Layout';

function Home() {
  return (
      <Router>
        <Layout/>
      </Router>
  );
}

export default Home
